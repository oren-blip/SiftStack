playwright>=1.40.0
2captcha-python>=1.2.0
python-dotenv>=1.0.0
apify>=2.0.0
google-api-python-client>=2.0.0
google-auth>=2.0.0
anthropic>=0.40.0
# OpenAI-compatible client — used ONLY to reach OpenRouter (and Ollama),
# never OpenAI itself. Carries the cheap high-volume jobs: heir alive/dead
# verification, Zillow page classification, lead-summary prose.
openai>=1.0
pdfminer.six>=20221105
smartystreets-python-sdk>=4.11.0
beautifulsoup4>=4.12.0
ddgs>=9.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
opencv-python-headless>=4.13.0
numpy>=1.26.0
dropbox>=12.0.2
openpyxl>=3.1.0
reportlab>=4.0.0

# Slack buttons for SMS draft approval (Socket Mode listener only; posting
# and chat.update go through plain requests). src/sms_agent/slack_buttons.py
slack_sdk>=3.44.1
